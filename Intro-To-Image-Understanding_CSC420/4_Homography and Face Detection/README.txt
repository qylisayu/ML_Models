All codes for the assignment are in Q4.py and Q5.py.

For Q4.py, change the constant CASE to 'A', 'B', or 'C' to run the tree different cases (default is CASE = 'A')

All output images are in the out folder

To get the output images, simply re-run the python files. All image paths should be correct given the directory set up as is submitted. 


To run the code, simply make the required imports as displayed at the top of the .py files, and all images files should be outputted to the appropriate question's folder in the out folder. 