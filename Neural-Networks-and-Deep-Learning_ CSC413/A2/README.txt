Using convolutional neural networks in image colourization. Given a greyscale image, we will predict the colour at each pixel.

Object detection by fine-tuning the YOLOv3 (Redmon and Farhadi, 2018) pre-trained model on the COCO (Lin et al., 2014) dataset.
